import React from "react";

export default function ReadingViewCentral({
  pageType,
  gridDirection,
  gridNumRows,
  pageNumer,
  widgets,
  pageData,
  onQuizResponse,
  savedResponses
}) {
  // Aquí iría la lógica y renderizado central, por ejemplo PageSelector
  // Puedes importar y usar PageSelector aquí
  return (
    <div className="reading-view-central">
      {/* TODO: Renderizar el contenido central del libro */}
    </div>
  );
}
